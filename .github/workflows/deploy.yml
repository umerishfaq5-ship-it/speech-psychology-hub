name: Deploy Site to FTP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗️ Build website
        run: npm run build

      - name: 🚀 Deploy to FTP
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER || 'ftp.clinicalspeechcenter.com' }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME || 'saba@clinicalspeechcenter.com' }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD || 'MknxW$y^}$z8zx0S' }}
        run: node scripts/deploy.mjs
